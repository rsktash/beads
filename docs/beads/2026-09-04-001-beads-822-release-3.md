# beads-822 — bd tool upgrade, Release 3 (features 15–22 + tracker hygiene)

Filed 2026-09-04 from a brainstorming session on the Workflow Fix Plan page
(section 13 contracts T17–T24). Release 3 ships the page's eight approved
features — `--since <handoff>` on the brief, brief ordering by binding
distance, `bd ready` ordered by the active plan, symbol-first citation
liveness (migration 0009), question expiry with the Decide gate, finding
back-links (0010), `bd comment retract` (0011) and the drop of the `design`
field (0012) — plus one tracker-hygiene task T0 that keys the sqlite
migration cache on file identity and rewrites the smoke-test runbook rule to
`--db <path>` with a fresh file name per run.

## Key decisions (owner, 2026-09-04)

- R-15 scope: features 15–22 plus T0.
- R-16 review mode: repeat R-14 — no per-task reviewer, one whole-diff review
  before close.
- R-17 shape: one epic, nine tasks, planned by writing-plans from the spec.
- Feature 20's real-tracker gate runs on a dumped copy in a throwaway
  postgres; the live apply and the same byte-count check are the owner's
  step at the gated reinstall.
- Release 3 is the first measured canonical run: pre-flight and whole-diff
  numbers are recorded against the Release 2 baseline (167K / 54 calls /
  12.5 min / 7 blockers + 26 notes).

## Acceptance

Nine tasks closed on their gates and merged to `main`; whole-diff review PASS;
repo gate exit 0; migrations 0009–0012 present with no number reused; T0's
recreated-file test green; the copy-based feature 20 check above 50,000 bytes
recorded on T24. Push and reinstall stay owner-gated.
